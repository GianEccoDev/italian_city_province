name: italian_city_province_helper
description: "A new Flutter package project."
publish_to: 'none'
version: 0.0.1
homepage: 'https://github.com/GianEccoDev'

environment:
  sdk: ^3.6.2
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
